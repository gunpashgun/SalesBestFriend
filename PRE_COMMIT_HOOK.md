# ✅ Pre-Commit Hook Setup

## 🛡️ Защита от ошибок

Настроен **автоматический pre-commit hook**, который проверяет TypeScript **перед каждым коммитом**.

## 🔧 Что он делает?

При каждом `git commit` автоматически:
1. 🔍 Запускает `npx tsc --noEmit`
2. ✅ Если нет ошибок → коммит проходит
3. ❌ Если есть ошибки → **коммит блокируется**

## 📝 Пример работы

```bash
$ git commit -m "Add new feature"

🔍 Running pre-commit checks...
📝 Checking TypeScript...

src/App.tsx(42,7): error TS2322: Type 'number' is not assignable to type 'string'.

❌ TypeScript check failed!
💡 Fix the errors above before committing.
```

## 🎯 Преимущества

- ✅ **Никаких ошибок TypeScript в Git**
- ✅ **Никаких провалов на Vercel**
- ✅ **Чистая история коммитов**
- ✅ **Работает автоматически**

## 🔧 Где находится?

Hook находится здесь: `.git/hooks/pre-commit`

## ⚠️ Важно

Если очень нужно сделать коммит **с ошибками** (для emergency fix):

```bash
git commit --no-verify -m "Emergency fix"
```

Но это **не рекомендуется**! 🚫

## 🎉 Результат

Теперь **ошибки TypeScript физически не смогут попасть в Git**, а значит и на Vercel! 🛡️

